# OS and editor noise
.DS_Store
._*
.idea/
.vscode/
*.cache
.php-cs-fixer.cache
*.map

# Dependencies and build output
/node_modules/
/vendor/
/var/
/.Build/
/public/
/.phpunit.cache/
composer.lock

# Generated reports and build intermediates. The pattern ends in `*` rather
# than `/`: git does not descend into an ignored DIRECTORY, so a negation
# inside one never matches — which is how the v0.6.2 report stayed untracked
# while the line re-including it sat right below.
/Build/Reports/*
# …except the upstream migration report. A design-review run is ephemeral;
# a record of what changed between two pinned Astryx releases is part of the
# release, and the CHANGELOG points at it.
!/Build/Reports/astryx-0.3.0-to-0.6.0.md
!/Build/Reports/astryx-0.6.0-to-0.6.2.md
/Build/Data/astryx-themes.generated.json

# The design review's URL list is instance-specific (uids and cHashes).
/Build/Data/design-review-urls.json
